<!DOCTYPE html>
<html lang="en">

<head>
    <title>Book List</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>

<body>

    <!-- 컨테이너 시작 -->
    <div class="container mt-3">
        <h2>Book List</h2>
        <hr />
        <!-- 검색 파트 시작-->
        <form action="/search" method="get">
            <div class="mb-3 mt-3">
                <input id="keyword" type="text" class="form-control" placeholder="Search" name="keyword" value="" />
            </div>
        </form>
        <button id="btn-search" type="button" class="btn btn-outline-danger mb-3">검색</button>
        <!-- 검색 파트 끝-->

        <!-- 테이블 시작 -->
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>id</th>
                    <th>title</th>
                    <th>content</th>
                    <th>createDate</th>
                </tr>
            </thead>
            <!-- 게시글 아이템 시작 -->
            <tbody>
                {{#posts}}
                <tr>
                    <td>{{id}}</td>
                    <td>{{title}}</td>
                    <td>{{content}}</td>
                    <td>{{createDate}}</td>
                </tr>
                {{/posts}}
            </tbody>
            <!-- 게시글 아이템 끝 -->
        </table>
        <!-- 테이블 끝 -->
    </div>
    <!-- 컨테이너 끝 -->

</body>

<script>

    // //1. 이벤트 리스너 
    // $("#btn-search").click((event) => {
    //     console.log(event);
    //     search();
    // });

    // // 2. 기능 - 검색 메서드
    // async function search(keyword) {

    //     let keywordDto = {
    //         keyword: $("#keyword").val()
    //     }
    //     console.log(keywordDto);

    //     let keywordJson = JSON.stringify(keywordDto);
    //     let keywordObject = JSON.parse(keywordJson);
    //     console.log(keywordObject);

    // }

</script>


</html>